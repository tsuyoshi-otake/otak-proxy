# 敵対的再監査・リリース判定

- 判定: **CONDITIONAL-GO**
- 実行記録: 2026-08-14T15:56:24.770Z
- C2: 100%
- mutation: 100%（surviving 0、equivalent 0）
- TLC: allPassed=true
- 対応表の整合性問題: 0
- 未検証acceptance requirement: なし

## Gate

| Gate | Result | Evidence |
| --- | --- | --- |
| GATE-COMMANDS | PASS | 全必須コマンドが終了コード0で完了 |
| GATE-PROCESS-CLEANUP | PASS | 最終runner-survivors=0 の証跡 |
| GATE-C2 | PASS | feasible C2=100%, unobserved=0 |
| GATE-MUTATION | PASS | score=100%, survived=0, equivalent=0 |
| GATE-TLA | PASS | allPassed=true, models=7 |
| GATE-TRACEABILITY | PASS | integrityProblems=0 |
| GATE-RELEASE-CRITICAL-SCOPE | PASS | 未検証のacceptance requirementなし |
| GATE-FRESHNESS | PASS | C2/mutation/TLA+のsource hashが現在の入力と一致 |

## 残存リスク・未検証範囲

- [medium] DVA-1.5: 不一致は未発生であり、実不一致の最小再現記録はcalibration以外では未取得。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-2.3: 代表的な無効／境界値はあるが、全入力形式の無効値体系は未網羅。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-2.4: オラクルは許可／禁止副作用を定義するが、全SUT adapterが実副作用列を返して比較する構造ではない。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-2.6: 不一致分類の自動artifactは未実装。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-3.7: runId/atomic writeは実装済みだが、並列競合を専用負荷テストしていない。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.1: port化した境界はGit/npm/shared filesystemに限定される。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.2: CLI、VS Code storage、shared filesystem、local proxyのプロトコル互換境界を検査する。watch/pollと多actorの実filesystem競合は未再現として残す。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.4: 実filesystemのatomic write/rename/破損復旧は確認済みだが、watch/pollと多actor競合の実filesystem結合は未実施。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.5: CLI、VS Code storage、shared filesystem、local proxyのプロトコル互換境界を検査する。watch/pollと多actorの実filesystem競合は未再現として残す。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.6: CLI、VS Code storage、shared filesystem、local proxyのプロトコル互換境界を検査する。watch/pollと多actorの実filesystem競合は未再現として残す。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-7.7: CLI、VS Code storage、shared filesystem、local proxyのプロトコル互換境界を検査する。watch/pollと多actorの実filesystem競合は未再現として残す。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.1: テスト内注入は決定的だが、共通FaultPlan台帳／解除条件の実装は未完了。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.2: 主要な0/1/上限境界はPBT/TLAで扱うが、全資源型のmax-1/max/max+1は未実施。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.3: 決定的fault injectionで資源、retry、部分失敗、event、cancel、crash/restartを検査する。未実装の資源型とprovider固有の故障形は残存リスクとして明示する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.4: retry上限、backoff、成功時resetは確認済み。Retry-After provider契約は未再現。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.5: 決定的fault injectionで資源、retry、部分失敗、event、cancel、crash/restartを検査する。未実装の資源型とprovider固有の故障形は残存リスクとして明示する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.6: stop後の遅延完了とproxy timeoutは確認済みだが、全cancel/timeout所有者を網羅していない。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.7: 決定的fault injectionで資源、retry、部分失敗、event、cancel、crash/restartを検査する。未実装の資源型とprovider固有の故障形は残存リスクとして明示する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.8: ENOSPC/EACCESは確認済み。EMFILE、lock枯渇、queue上限の実装結合注入は未実施。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.9: 決定的fault injectionで資源、retry、部分失敗、event、cancel、crash/restartを検査する。未実装の資源型とprovider固有の故障形は残存リスクとして明示する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-8.10: 決定的fault injectionで資源、retry、部分失敗、event、cancel、crash/restartを検査する。未実装の資源型とprovider固有の故障形は残存リスクとして明示する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.1: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.2: 複数actor/in-flight/event競合はモデル化済み。複数targetと全永続境界の状態は未抽象化。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.3: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.4: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.5: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.6: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.7: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-9.8: 実装非依存のlifecycle/sync有限抽象。targetごとの永続境界モデルは次段階。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.1: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.2: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.3: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.4: actor/retry/resource/timeは変化させたが、target数の定数行列は未実装。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.5: resource/epoch/単調replica/禁止状態を検査。credential値と実lock相互排他はTLAで未モデル化。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.6: Checking→Applied/Stopped/Failed/Crashedとsync収束を検査。partial/awaitingUser/cancelled全terminal集合は未モデル化。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.7: TLC deadlock検査は有効だが、抽象モデルはPulse Actionでterminalを進行可能に表現する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.8: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.9: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-10.10: 固定seedのTLC BFS、Safety/Liveness/deadlock、期待反例到達性を記録する。無制限定数とcredential値は未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.1: 機械検査可能な対応表と敵対的再監査を生成する。release gateは制御済み検証の結果と明示した残存リスクに基づき判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.2: 機械検査可能な対応表と敵対的再監査を生成する。release gateは制御済み検証の結果と明示した残存リスクに基づき判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.3: 機械検査可能な対応表と敵対的再監査を生成する。release gateは制御済み検証の結果と明示した残存リスクに基づき判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.4: 機械検査可能な対応表と敵対的再監査を生成する。release gateは制御済み検証の結果と明示した残存リスクに基づき判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.5: C2/mutation及びSafety gateの実行結果は敵対的監査で判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.7: 有限探索とtest doubleの抽象化差はCONDITIONAL-GOの残存リスクとして監査結果へ記録する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] DVA-12.8: 機械検査可能な対応表と敵対的再監査を生成する。release gateは制御済み検証の結果と明示した残存リスクに基づき判定する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] environment-harness: VS Code host実行時のharness診断は environment-observations.md に記録した。テスト結果とcleanupは成功したが、ランチャー更新時に再確認する。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] model-abstraction: TLA+ は有限actor/時刻/resource/epoch/queueの抽象であり、無制限の値空間、credential値、実lock相互排他、全永続化targetは未探索。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer
- [medium] environment-assumption: sync livenessは信頼配送（MaxDrops=0）と明示したweak fairnessの仮定に依存する。loss-boundary反例はこの依存を確認している。
  - 影響: 保証の一般化範囲が狭く、特定環境又は未探索入力で差異が生じうる。
  - 発生可能性: 有限探索外、provider差、又は未再現の故障条件で発現しうる。
  - 検出可能性: 運用監視、既存の診断、又は追加の境界テストで検出可能だが、現行gateだけでは完全ではない。
  - 軽減策: 抽象化差を監視し、対象が変わる際は境界テスト／TLC定数行列を拡張して再監査する。
  - owner: release owner と対象コンポーネントのmaintainer

## 判定根拠

制御済みの検証gateは充足している。有限探索、test doubleと実環境の差、未採用の入力領域を条件・監視・rollbackを伴う残存リスクとして扱うため、判定はCONDITIONAL-GOとする。
